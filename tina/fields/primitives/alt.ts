import { type TinaField } from "tinacms";

export const alt: TinaField = {
  description: "Text to display when the user has images off.",
  label: "Alternative text",
  name: "alt",
  type: "string",
};
